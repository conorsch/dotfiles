# Config file for ghostty. See docs at:
# https://ghostty.org/docs/config
#
# Reload config via ctrl+shift+,

# Config syntax crash course
# ==========================
# # The config file consists of simple key-value pairs,
# # separated by equals signs.
# font-family = Iosevka

# As was the case for Alacritty, setting monospace font family by itself
# isn't enough; see also `~/.config/fontconfig/fonts.conf`,
# which forces use of an emoji-capable font.
font-family = Monospace
font-size = 8
# window-padding-x = 2

# disable window borders, to gel with i3.
window-decoration = none


# Set color scheme and theme.
# View themes bundled with ghostty via: `ghostty +list-themes`
# Some favorites are:
# theme = Abernathy
# theme = Argonaut
# theme = Night Lion V2
# theme = Oxocarbon
# theme = Dark+
# theme = Vercel

# However it's the out of band warm-burnout that seems comfy:
# theme = dark:warm-burnout-dark,light:warm-burnout-light
theme = warm-burnout-dark

# copy/paste clipboard settings
copy-on-select = false
keybind = performable:ctrl+shift+c=copy_to_clipboard
keybind = performable:ctrl+shift+v=paste_from_clipboard
# app-notifications = no-clipboard-copy
app-notifications = no-clipboard-copy

# don't prompt on i3 window kill
confirm-close-surface = false

# performance tweaks, via https://michaelbommarito.com/wiki/programming/tools/ghostty-configuration/
gtk-single-instance = desktop
# ~100mb per terminal
scrollback-limit = 100000000

# harcode bash integration, to minimize blocking via "auto" setting
shell-integration = bash

# enable terminal transparency. for i3, transparency is handled by picom compositor;
# sway does not support compositors, though, so terminal app must do it.
background-opacity = 0.90

# start a login shell, to activate features like multiplexer resumption
command = "bash -l"
