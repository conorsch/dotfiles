# This config file specifies paths that should be excluded
# from chezmoi's dotfiles handling; anything in this list
# will *not* be naively copied to HOME.

# general dev tooling for dotfiles repo
Makefile
README.md
bin/chezmoi
bootstrap.sh
dom0/
justfile
packages/

# nix config
flake.nix
flake.lock
home.nix
result

# additional tooling, to be built by nix
tools/

# swayfx config; documenting it in-repo, but not using it currently
dot_config/sway/config.d/swayfx.conf
